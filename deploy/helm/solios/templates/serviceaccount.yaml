{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "solios.serviceAccountName" . }}
  namespace: {{ .Values.namespace }}
  labels:
{{ include "solios.labels" . | nindent 4 }}
{{- end -}}
